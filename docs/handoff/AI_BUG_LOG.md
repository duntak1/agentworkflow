# AI / 会话 Bug 流水

**用途：** 记录测试失败或用户口述问题；跨会话追溯。

**约定（见 `AGENT_RULES.md`）：** 发现后追加一行（日期、来源、状态、摘要）；修复后标 `done` 或链 Issue/REQ。

| 字段 | 说明 |
|------|------|
| **来源** | `test` · `chat` · `hook-*` |
| **状态** | `open` / `investigating` / `done` / `wontfix` |

重大缺陷请建 GitHub Issue 或 `aw req new`。

---

## 流水（新在上）

_（暂无 — 本仓库为 agent-workflow 通用包；历史业务项目日志已清除）_
