# 功能测试用例（书面）

见 `agent-workflow/VERSION_CHANGELOG_QUALITY_LOOP.md` §11。

**脚手架：** `./scripts/aw tp new <slug> "标题"` → `TP-YYYYMMDD-NN-<slug>.md`，并更新 `INDEX.md`。

**与 AT-T 绑定：** `./scripts/aw tp link <AT-T> <TP>`（Verify 列追加 `TP:docs/quality/test-plans/…`）。
