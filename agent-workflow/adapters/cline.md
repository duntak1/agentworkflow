# Cline（VS Code 扩展）

## 挂载

- 项目根 **`.clinerules`** — `./scripts/install-aw-adapters.sh --cline`

## 触发

与 [`INVOCATION.md`](../INVOCATION.md) 一致；首条可贴 `./scripts/aw paste session`。

## 说明

Cline 无统一全局 Skill 目录；以 **仓库内规则文件 + `@` 文件** 为准。复杂流程读 `agent-workflow/PRODUCT_INPUT_WORKFLOW.md`。
