# Continue（VS Code / JetBrains 等）

## 挂载

- **`.continue/rules/agent-workflow.md`** — `./scripts/install-aw-adapters.sh --continue`
- 或在 Continue 设置中将该文件加入 **Workspace rules**

## 触发

`按 AI 工作流` · `aw status` · 上下文附带 `agent-workflow/INVOCATION.md`

## 限制

Continue 配置因版本而异；若规则未自动加载，在对话中 `@.continue/rules/agent-workflow.md`。
